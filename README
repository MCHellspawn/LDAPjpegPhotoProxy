# LDAP jpegPhoto Proxy

This is a simple python script that will connect to an LDAP directory and retrieve a binary attribute and serve it as a jpeg image file (not and html file with an image tag). This is useful when you need to expose a user's image from LDAP as a jpeg for use in exterlan site profiles. 